# 05 目录结构

单 Next.js 应用 + 内部分层（Monorepo 拆包预留，依赖规则见架构文档）。

```
text02/
├─ src/
│  ├─ app/                          # Next.js App Router
│  │  ├─ layout.tsx / page.tsx      # 根布局；/ 按会话重定向 login|dashboard
│  │  ├─ globals.css                # Tailwind 4 入口
│  │  ├─ login/page.tsx             # 登录/注册（客户端）
│  │  ├─ (app)/                     # 需登录的应用区（layout 服务端鉴权）
│  │  │  ├─ layout.tsx              # 侧边栏 + 用户信息 + 退出
│  │  │  ├─ logout-button.tsx
│  │  │  ├─ dashboard/page.tsx      # 仪表盘（漏斗/趋势/到期提醒）
│  │  │  ├─ resumes/
│  │  │  │  ├─ page.tsx             # 简历列表：新建/上传解析/设主简历/删除
│  │  │  │  └─ [id]/page.tsx        # 编辑器：版本切换/结构化编辑/AI 优化/采纳
│  │  │  ├─ jobs/page.tsx           # 职位：JD 粘贴 AI 解析/收藏/匹配
│  │  │  ├─ applications/page.tsx   # 看板：阶段流转/时间线/备注/建提醒
│  │  │  └─ reminders/page.tsx      # 提醒：待办/到期/完成/忽略
│  │  └─ api/v1/                    # REST API（薄控制器）
│  │     ├─ auth/{register,login,refresh,logout,me}/route.ts
│  │     ├─ users/me/profile/route.ts
│  │     ├─ resumes/{route,[id],versions,upload,optimize,analysis}
│  │     ├─ jobs/{route,[id],parse,favorite}
│  │     ├─ matches/{route,[id]}
│  │     ├─ applications/{route,[id],stage,events,board}
│  │     ├─ reminders/{route,[id]}
│  │     ├─ ai-tasks/[id]/route.ts
│  │     └─ dashboard/overview/route.ts
│  ├─ server/
│  │  ├─ core/                      # 领域服务层
│  │  │  ├─ auth-service.ts         # 注册/登录/公开视图
│  │  │  ├─ user-service.ts         # 画像
│  │  │  ├─ resume-service.ts       # 简历/版本/上传落库/分析
│  │  │  ├─ file-parser.ts          # txt/pdf/docx → 文本
│  │  │  ├─ job-service.ts          # 职位/公司建档/收藏/筛选
│  │  │  ├─ match-scorer.ts         # 规则评分器（纯函数）
│  │  │  ├─ match-service.ts        # 匹配编排/upsert/列表
│  │  │  ├─ application-service.ts  # 投递状态机/时间线/看板
│  │  │  ├─ reminder-service.ts     # 提醒
│  │  │  └─ dashboard-service.ts    # 看板聚合
│  │  └─ ai/
│  │     ├─ gateway.ts              # 限额→重试→JSON解析→计量
│  │     ├─ runner.ts               # 任务状态机 + 各类型处理器（含持久化）
│  │     ├─ prompts.ts              # 4 类场景 Prompt 模板
│  │     └─ providers/
│  │        ├─ mock.ts              # 确定性 Mock（含 JD/简历扫描器）+ Provider 接口
│  │        └─ openai-compat.ts     # GLM/DeepSeek/Qwen 兼容端点
│  ├─ db/
│  │  ├─ schema.ts                  # 全量表定义（唯一数据模型处）
│  │  ├─ client.ts                  # 惰性单例 + 自动迁移
│  │  └─ seed.ts                    # 演示数据（幂等）
│  ├─ shared/                       # 前后端共享
│  │  ├─ types.ts                   # 领域类型/阶段状态机/常量
│  │  ├─ schemas.ts                 # Zod 校验（请求体/查询）
│  │  ├─ errors.ts                  # 错误码 + AppError + HTTP 映射
│  │  └─ response.ts                # 统一信封 ok/fail
│  ├─ lib/
│  │  ├─ auth.ts                    # JWT/刷新令牌/Cookie/会话
│  │  ├─ api.ts                     # defineRoute 路由包装器
│  │  ├─ session.ts                 # 服务端组件读会话
│  │  └─ client.ts                  # 前端 fetch 封装 + 任务轮询
│  ├─ components/modal.tsx          # 共享 UI
│  ├─ types/pdf-parse.d.ts
│  └─ testing/helpers.ts            # 测试工具：callRoute/resetDb/createTestUser
├─ drizzle/                         # 迁移 SQL（db:generate 产出）
├─ data/                            # SQLite 库 + uploads（gitignore）
├─ docs/                            # 本设计文档
├─ *.config (next/ts/drizzle/vitest/postcss)
└─ package.json                     # dev/build/test/db:generate/db:seed
```

## 分层规则

1. `app/api/**` 只做：参数校验（Zod）→ 调用 core 服务 → 包装响应；不出现 SQL 与业务规则。
2. `server/core/**` 持有全部业务规则（所有权校验、状态机、聚合），可依赖 db/shared；不依赖 runner。
3. `server/ai/runner.ts` 是唯一编排 AI 任务持久化的地方（依赖 core 纯函数与 db）。
4. `shared/**` 零依赖，前后端同构使用。
5. 测试与源码同目录（`__tests__` / `*.test.ts`），随模块演进。
